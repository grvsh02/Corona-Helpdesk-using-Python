Prerequisite:-
1) Python 3 installed with proper rights
2) python 3 added to PATH variables


Steps to execute the program :-
1) Install all the required libraries using cmd
       <code>pip install * </code>
	where * is the name of the library

	Libraries/packages used are:
		1) matplotlib
		2) pyside2
		3) pyQt5
		4) time
		5) Platform
		6) numpy
		7) mysql. Connector

2) Run the main.py (all other programs are interlinked)
		
Note:- The following credentials can be used to login to our database window
	username:- admin
	password:- 123456

